@model IEnumerable<Serwis_Muzyczny.Models.wykonanie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.artysta.pseudonim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.utwor.nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.artysta.pseudonim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.utwor.nazwa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.wykonanieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.wykonanieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.wykonanieId })
        </td>
    </tr>
}

</table>
