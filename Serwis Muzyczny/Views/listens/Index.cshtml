@model IEnumerable<Serwis_Muzyczny.Models.odsluch>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dataOdtworzenia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.utwor.nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.uzytkownik.imie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dataOdtworzenia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.utwor.nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.uzytkownik.imie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.odsluchId }) |
            @Html.ActionLink("Details", "Details", new { id=item.odsluchId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.odsluchId })
        </td>
    </tr>
}

</table>
